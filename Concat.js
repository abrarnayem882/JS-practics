const first = 'Abrar';
const last = 'Nayem';

const fullname = first +' ' +last ;
// console.log(fullname);

const Fullname = first.concat(' ').concat(last);
console.log(Fullname);

//includes
console.log(last.includes('W'))